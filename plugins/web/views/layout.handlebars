<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="icon" href="favicon.ico">

    <title>HomeCtrl</title>

    <!-- Bootstrap core CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="/static/layout.css" rel="stylesheet">

    <!-- jQuery first, then Tether, then Bootstrap JS. -->
    <script src="https://code.jquery.com/jquery-3.1.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/tether/1.3.7/js/tether.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/js/bootstrap.min.js"></script>
    <script src="/static/utils.js"></script>
</head>

<body>
    <nav class="navbar navbar-toggleable-md navbar-inverse fixed-top bg-inverse">
        <button class="navbar-toggler navbar-toggler-right hidden-lg-up" type="button" data-toggle="collapse" data-target="#navbarsDefault"
            aria-controls="navbarsDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <a class="navbar-brand" href="/">HomeCtrl</a>

            <div class="collapse navbar-collapse" id="navbarsDefault">
                <ul class="navbar-nav mr-auto">
                    {{#if isHome}}
                    <li class="nav-item active">
                    {{else}}
                    <li class="nav-item">
                    {{/if}}
                        <a class="nav-link" href="/">Home</a>
                    </li>

                    {{#unless isHome}}
                    <li class="nav-item active">
                    {{else}}
                    <li class="nav-item">
                    {{/unless}}
                        <a class="nav-link" href="/log">Log</a>
                    </li>
                </ul>
                <h3><span id="badgeConnectionStatus" class="badge badge-danger">Disconnected</span></h3>
            </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <nav class="col-sm-3 col-md-2 hidden-xs-down bg-faded sidebar">
                <ul class="nav nav-pills flex-column">
                    <li class="nav-item">
                        <span class="nav-link"><h5>Plugins</h5></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Sonos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Pushover</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Hue</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Doorbell</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Web</a>
                    </li>
                    {{#each plugins}}
                    <li class="nav-item">
                        <a class="nav-link" href="#">{{this.name}}</a>
                    </li>
                    {{/each}}
                </ul>
            </nav>

            <main class="col-sm-9 offset-sm-3 col-md-10 offset-md-2 pt-3">
                {{{body}}}
            </main>
        </div>
</body>

</html>